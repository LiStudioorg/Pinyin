# PNY 语言规范 v1.0

## 概述

PNY 是一种极简的命令式编程语言，设计用于教学和实验。

## 文件格式

- 文件后缀：`.pny`
- 编码：UTF-8
- 入口：`hanshu main() { ... }`

## 注释

```
// 这是注释，到行尾结束
```

## 关键字

| 关键字  | 含义     |
|---------|----------|
| hanshu  | 函数声明 |
| fanhui  | 返回     |
| ling    | 变量声明 |
| ruguo   | 如果     |
| fouze   | 否则     |
| dang    | 循环     |
| shuchu  | 打印     |

## 类型

- `int`：32 位有符号整数（内部用 int32_t）
- 字符串字面量：`"..."`，只能在 `shuchu(...)` 中使用

## 变量

- 使用 `ling` 声明：`ling x = 表达式`
- 赋值（不带 `ling`）：`x = 表达式`
- 所有变量都是 int 类型

## 运算符

### 算术运算符

| 运算符 | 含义 |
|--------|------|
| `+`    | 加   |
| `-`    | 减   |
| `*`    | 乘   |
| `/`    | 除（整数除法） |

### 比较运算符

| 运算符 | 含义 |
|--------|------|
| `==`   | 等于 |
| `!=`   | 不等于 |
| `<`    | 小于 |
| `>`    | 大于 |
| `<=`   | 小于等于 |
| `>=`   | 大于等于 |

### 一元运算符

| 运算符 | 含义 |
|--------|------|
| `-`    | 取负 |

### 优先级（低到高）

1. `==` `!=` `<` `>` `<=` `>=`
2. `+` `-`
3. `*` `/`
4. 一元负号 `-`
5. 括号 `(...)`

## 语法

### 函数声明

```
hanshu 函数名(参数列表) {
    语句...
}
```

- 参数列表可以为空：`hanshu main() { ... }`
- 参数都是 int 类型
- 前 8 个参数通过寄存器 x0-x7 传递

### 变量声明

```
ling 变量名 = 表达式
```

### 赋值

```
变量名 = 表达式
```

### 打印

```
shuchu("字符串字面量")
shuchu(整数表达式)
```

### 条件语句

```
ruguo 条件表达式 {
    语句...
} fouze {
    语句...
}
```

- `fouze` 部分可省略
- 支持嵌套

### 循环语句

```
dang 条件表达式 {
    语句...
}
```

- 条件为真（非零）时循环

### 返回语句

```
fanhui 表达式
```

## 示例

```
hanshu main() {
    ling a = 1 + 2 * 3
    ling b = (a + 4) / 2
    ling c = -b
    ruguo a > b {
        shuchu("a da")
    } fouze {
        shuchu("b da")
    }
    ling i = 0
    dang i < 3 {
        shuchu(i)
        i = i + 1
    }
    shuchu(jia(2, 3))
}

hanshu jia(x, y) {
    fanhui x + y
}

hanshu fib(n) {
    ruguo n < 2 { fanhui n }
    fanhui fib(n-1) + fib(n-2)
}
```

## 语句结束

分号 `;` 在语句末尾可选。

## 错误处理

编译器会输出中文错误信息，包含行号和列号：

```
错误: xxx, 位置 行:列
```
